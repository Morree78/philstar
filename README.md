# Business Desk

- `dist/`: deployable static website, including the updated Philstar navigation links.
- `laravel/`: Laravel application source. See [setup instructions](laravel/README.md).
- `.openai/hosting.json`: existing Sites project configuration.

Serve `dist/` with a static web server. The Laravel application requires separate PHP hosting.

This is an independent reader, not an official Philstar website. Publisher content retains source attribution; the Lulu announcement is user-supplied and has not been independently verified.
